# LM Market Bot 0.3.3 — release candidate

Not published or approved for deployment.

- Independent MUD, ONI and USTUR Windows installations, executables, installer identities, update caches and feeds.
- Native installer relaunch replaces the shared restart-helper approach.
- Profile-specific single-instance protection and supervisor attachment after native relaunch.
- Existing profile settings and encrypted secrets retain their storage paths.
- Production entrypoint has no test-source override; development routing module excluded from packages.

## One-time migration
Do not use the legacy shared installation's Update button to perform the split. Back up profile settings/secrets, task definitions and launchers. Install dedicated packages to isolated directories, never overwrite or uninstall the shared folder while a sibling uses it. Migrate MUD, then ONI, then USTUR, checking identity, settings, unique main process, supervision and normal runtime after each. Preserve original shared files for rollback. Old MUD test registration/directory must be reconciled explicitly before production migration; do not blindly uninstall and risk profile data.

## Evidence and limits
Development MUD .3→.4 updater/native restart/supervisor attachment passed a live five-minute test; original production restored with protected hashes and sibling process IDs unchanged. That validates mechanics, not final0.3.3 payloads or official GitHub discovery. Native relaunch took approximately156 seconds against180-second observer deadline: limited timing margin is a release review item. Official GitHub path must be verified on a later real update. Full production artifact inspection and Windows supervisor fixture remain release gates.

Expected assets: three profile-specific installers, three blockmaps and mud.yml/oni.yml/ustur.yml. Publish only the reviewed immutable versions/hashes, after explicit approval. No tag, GitHub release or live migration is implied by this preparation.

## Preparation verification (2026-09-21)
- Full suite106/106 passed; subsequent added packaging exclusion guard2/2 focused passed. Typecheck and diff checks passed.
- Native Windows mocked supervisor fixture4/4 passed (not a new live deployment).
- Local WSL installer build blocked: electron-rebuild/node-gyp cannot cross-compile bigint-buffer from source. No final0.3.3 installers produced; use native Windows CI, do not disable dependency rebuilding to conceal this limitation.
- Review: official-feed routing restored, test module excluded, no profile/secret storage change, release assets isolated by faction; test-only CI option removed. Historical test builder deliberately refuses use after production routing removal. Existing development test evidence remains on dad0406.
- Pending: approved development-branch push and Windows CI artifact build/inspection; full release merge/review and publication approval; later live migration approval. No main/tag/release changes made.
